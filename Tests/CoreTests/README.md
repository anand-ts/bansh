# Core Tests

This folder contains regression coverage for the portable transliteration engine.

Current test paths:

- `BanshCoreTests.mm` is the Xcode `XCTest` target for routine local development
- `TransliterationSessionTests.cpp` remains a plain C++ harness for engine-only validation
